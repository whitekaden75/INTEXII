
// This file is kept for backward compatibility but is no longer used
// We're now using sonner instead
import { toast } from "sonner";

export function Toaster() {
  return null;
}

export { toast };
